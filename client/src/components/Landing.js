import React from "react";

export const Landing = () => {
    return (
        // 2 curly braces: first for js and second is actually an object
        <div style={{textAlign:'center'}}>
            <h1>Emaily</h1>
            Collect feedback from your users
        </div>
    );
};
